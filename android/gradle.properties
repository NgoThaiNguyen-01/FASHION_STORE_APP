org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# If your build fails due to incompatible Java version, install JDK 21 (LTS) and
# uncomment and set the following property to the JDK 21 installation path.
# Example on Windows:
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.1+9\\

# On CI, ensure the runner uses JDK 21 or set JAVA_HOME to the JDK 21 path before running Gradle.
